{
  "name": "insight-hold-melt",
  "version": "1.0.0",
  "private": true,
  "description": "InSight Hold Melt Dashboard - PERC registration hold tracking and visualization for Champlain College's InSight program",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-data": "python3 scripts/build-data.py",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
